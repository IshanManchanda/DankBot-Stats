[project]
name = ""
version = ""
description = ""
authors = [
    {name = "Ishan Manchanda", email = "ishanmanchanda70@gmail.com"},
]
dependencies = ["dnspython==2.1.0", "Flask==1.1.2", "gunicorn==20.1.0", "pymongo==3.11.4", "pypertrail==1.0.0", "python-dotenv==0.17.1", "pytz==2021.1", "redis==3.5.3", "requests==2.25.1", "rq==1.8.1"]
requires-python = ">=3.9"
dynamic = ["classifiers"]
license = {text = "MIT"}

[project.urls]
homepage = ""

[build-system]
requires = ["pdm-pep517"]
build-backend = "pdm.pep517.api"


[tool]
[tool.pdm]
[[tool.pdm.source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[tool.pdm.dev-dependencies]
dev = []
